readme.txt

the files in in this repo are my projects all written in C.

the files include a server, a client and a database.

the mdb-lookup file searchs the database and prints out users input.

all files are written in c without preexisting API's 
